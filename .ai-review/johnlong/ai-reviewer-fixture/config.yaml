global_instructions: |
  Repo-specific instructions should override the global config when present.
  Pay extra attention to branch-specific personas.

model_mapping:
  balanced:
    provider: openai
    model: gpt-5
    max_tokens: 6000
    reasoning_level: medium
    input_price_per_million: 1.25
    output_price_per_million: 10.00
